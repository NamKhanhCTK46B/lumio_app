{
  "framework": "nextjs",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm build",
  "installCommand": "pnpm install",
  "devCommand": "pnpm dev",
  "regions": ["sin1"],
  "functions": {
    "src/app/api/speech/**": {
      "runtime": "nodejs20.x"
    }
  }
}